import { OptionsBgDirective } from './options-bg.directive';

describe('OptionsBgDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionsBgDirective();
    expect(directive).toBeTruthy();
  });
});
